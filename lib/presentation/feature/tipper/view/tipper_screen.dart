import 'package:flutter/material.dart';

class TipperScreen extends StatelessWidget {
  const TipperScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Tipper Screen'),
      ),
    );
  }
}
